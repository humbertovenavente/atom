---
phase: 02-flow-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - src/styles.css
  - src/app/app.config.ts
  - src/app/app.ts
  - src/app/services/flow.service.ts
  - src/server/routes/api/flow.get.ts
autonomous: true
requirements: [SERV-02]

must_haves:
  truths:
    - "FlowService injectable with signal-based state for nodes, edges, activeNodeId, selectedNodeId"
    - "FlowService.loadDefaultFlow() fetches GET /api/flow and populates nodes/edges signals"
    - "GET /api/flow returns JSON with nodes[] and edges[] arrays representing the 8-node default flow"
    - "@foblex/flow is installed and FFlowModule is importable without build errors"
    - "Tailwind CSS v4 utility classes are applied in the app"
    - "SSR is disabled — no window/document reference errors"
  artifacts:
    - path: "src/app/services/flow.service.ts"
      provides: "Signal-based FlowService with nodes, edges, activeNodeId, selectedNodeId"
      exports: ["FlowService"]
    - path: "src/server/routes/api/flow.get.ts"
      provides: "Mock GET /api/flow returning default 8-node agent pipeline"
    - path: "src/styles.css"
      provides: "Tailwind v4 import and @foblex/flow base styles"
  key_links:
    - from: "src/app/services/flow.service.ts"
      to: "/api/flow"
      via: "HttpClient GET request"
      pattern: "http\\.get.*api/flow"
    - from: "src/app/app.config.ts"
      to: "@angular/common/http"
      via: "provideHttpClient"
      pattern: "provideHttpClient"
---

<objective>
Install @foblex/flow and Tailwind CSS, disable SSR, create FlowService with Angular Signals as single source of truth for all flow editor state, and create the mock GET /api/flow endpoint returning the default 8-node agent pipeline.

Purpose: FlowService is the foundation for all canvas rendering (Plan 02-02) and sidebar interaction (Plan 02-03). Without the service and mock API, the canvas has no data to render.

Output: FlowService injectable, mock /api/flow endpoint, @foblex/flow + Tailwind installed and configured.
</objective>

<execution_context>
@/home/jose/.claude/get-shit-done/workflows/execute-plan.md
@/home/jose/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-flow-editor/02-CONTEXT.md
@.planning/phases/02-flow-editor/02-RESEARCH.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md

<interfaces>
<!-- Key types from src/shared/types.ts that FlowService depends on -->

From src/shared/types.ts:
```typescript
export interface FlowNode {
  id: string;
  type: 'memory' | 'orchestrator' | 'validator' | 'specialist' | 'generic' | 'tool';
  position: { x: number; y: number };
  data: {
    label: string;
    icon: string;
    color: string;
    config?: NodeConfig;
  };
}

export interface FlowEdge {
  id: string;
  source: string;
  target: string;
  label?: string;
  animated?: boolean;
}

export interface FlowConfig {
  flowId: string;
  nodes: FlowNode[];
  edges: FlowEdge[];
  nodeConfigs: Record<string, NodeConfig>;
}
```
</interfaces>

<codebase_state>
IMPORTANT: The current codebase does NOT have @foblex/flow or Tailwind CSS installed (not in package.json). SSR is still enabled (ssr: true in vite.config.ts). There is no @models/* path alias in tsconfig.json. The app.config.ts still has provideClientHydration. The app.ts has inline styles with max-width/margin that need removal for full-viewport layout. These foundation gaps MUST be resolved in Task 1 before any flow editor work.

Current package.json has Angular 21, Analog.js 2.3, MongoDB/Mongoose, but no @foblex/flow, no @tailwindcss/*.

Existing server routes: src/server/routes/api/chat.post.ts (SSE chat), src/server/routes/api/v1/hello.ts. No flow.get.ts exists.
</codebase_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @foblex/flow + Tailwind CSS, disable SSR, configure path alias</name>
  <files>package.json, vite.config.ts, tsconfig.json, src/styles.css, src/app/app.config.ts, src/app/app.ts</files>
  <action>
1. Install @foblex/flow: Run `cd /home/jose/atom && npx ng add @foblex/flow --skip-confirmation` (the ng add schematic configures imports automatically). If ng add fails, fall back to `npm install @foblex/flow` and manually verify FFlowModule is importable.

2. Install Tailwind CSS v4: Run `npm install -D @tailwindcss/vite @tailwindcss/postcss`. Note: Tailwind v4 uses CSS-first configuration, no tailwind.config.js needed.

3. Update `vite.config.ts`:
   - Import `tailwindcss` from `@tailwindcss/vite`
   - Add `tailwindcss()` to the plugins array
   - Change `ssr: true` to `ssr: false` in the analog() plugin config (CRITICAL — prevents window/document errors from @foblex/flow)
   - Remove the `nitro: { preset: 'vercel' }` line (not needed in dev, will be added back for deploy)

4. Update `tsconfig.json`:
   - Add path alias `"@models/*": ["src/shared/*"]` under `compilerOptions.paths` so types can be imported as `@models/types`

5. Update `src/styles.css`:
   - Add `@import 'tailwindcss';` at the top
   - Add full-viewport reset: `html, body { display: block; height: 100%; margin: 0; }`
   - Add @foblex/flow base styles: `.f-flow { width: 100%; height: 100%; }` and `.f-canvas { background: #111827; }` (gray-900 dark canvas)

6. Update `src/app/app.config.ts`:
   - Remove `provideClientHydration(withEventReplay())` — incompatible with SSR disabled
   - Keep `provideHttpClient(withFetch(), withInterceptors([requestContextInterceptor]))`

7. Update `src/app/app.ts`:
   - Remove the inline `styles` block that sets max-width/margin/padding/text-align (prevents full-viewport layout)
   - The component should just be: `template: '<router-outlet />'` with no constraining styles

8. Create `.postcssrc.json` at project root with: `{ "plugins": { "@tailwindcss/postcss": {} } }`

9. Verify: Run `npm run build` — must succeed without errors. Check that `import { FFlowModule } from '@foblex/flow'` is resolvable.
  </action>
  <verify>
    <automated>cd /home/jose/atom && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>@foblex/flow installed and importable, Tailwind CSS v4 configured with CSS-first approach, SSR disabled, @models/* path alias works, app builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create FlowService with Angular Signals and mock GET /api/flow endpoint</name>
  <files>src/app/services/flow.service.ts, src/server/routes/api/flow.get.ts</files>
  <action>
1. Create `src/server/routes/api/flow.get.ts` — Mock GET endpoint returning the default 8-node agent pipeline:
   - Use `defineEventHandler` from 'h3' (established pattern from chat.post.ts)
   - Return a JSON object: `{ nodes: FlowNode[], edges: FlowEdge[] }`
   - 8 nodes covering all 6 types: orchestrator-1, memory-1, specialist-faqs, specialist-catalog, specialist-schedule, validator-1, generic-1, tool-search
   - Node positions forming a left-to-right pipeline layout (orchestrator at left, fan out to specialists in middle, converge at validator/generic on right)
   - Each node has `data: { label, icon, color }` matching these assignments:
     - memory: label "Memoria", icon "brain", color "#8B5CF6" (purple)
     - orchestrator: label "Orquestador", icon "target", color "#3B82F6" (blue)
     - validator: label "Validador", icon "check-circle", color "#10B981" (green)
     - specialist: label "Especialista [topic]", icon "zap", color "#F59E0B" (amber)
     - generic: label "Genérico", icon "message-circle", color "#6B7280" (gray)
     - tool: label "Herramienta [name]", icon "tool", color "#EF4444" (red)
   - 8 edges connecting the pipeline: orchestrator → memory, orchestrator → each specialist, specialists → validator, validator → generic, generic → tool-search
   - Use the FlowEdge interface from `../../../shared/types` with `source` and `target` fields (NOT sourceId/targetId — the interface uses `source`/`target`)

2. Create `src/app/services/flow.service.ts`:
   - Injectable with `{ providedIn: 'root' }`
   - Import `signal`, `inject` from `@angular/core`
   - Import `HttpClient` from `@angular/common/http`
   - Import `FlowNode`, `FlowEdge` from `@models/types` (using the path alias)
   - Signal-based state:
     - `nodes = signal<FlowNode[]>([])`
     - `edges = signal<FlowEdge[]>([])`
     - `activeNodeId = signal<string | null>(null)`
     - `selectedNodeId = signal<string | null>(null)`
   - Methods:
     - `loadDefaultFlow(): void` — GET /api/flow, subscribe to set nodes and edges signals. Include error handler with console.error.
     - `addNode(node: FlowNode): void` — append to nodes signal via update()
     - `updateNodePosition(id: string, position: { x: number; y: number }): void` — map over nodes, update matching node's position
     - `setActiveNode(id: string | null): void` — set activeNodeId signal
     - `setSelectedNode(id: string | null): void` — set selectedNodeId signal
     - `removeNode(id: string): void` — filter node from nodes signal, also remove edges connected to that node
     - `addEdge(edge: FlowEdge): void` — append to edges signal

IMPORTANT: The FlowEdge interface uses `source` and `target` (not sourceId/targetId). Make sure the mock data and service methods align with the actual interface in src/shared/types.ts.
  </action>
  <verify>
    <automated>cd /home/jose/atom && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>FlowService exports signal-based state (nodes, edges, activeNodeId, selectedNodeId) with loadDefaultFlow() method that fetches from /api/flow. GET /api/flow returns 8 nodes and 8 edges. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- `src/app/services/flow.service.ts` exists and exports FlowService with signal-based state
- `src/server/routes/api/flow.get.ts` exists and returns JSON with nodes[] and edges[]
- `@foblex/flow` is in package.json dependencies
- `@tailwindcss/vite` is in package.json devDependencies
- `vite.config.ts` has `ssr: false`
- `tsconfig.json` has `@models/*` path alias
</verification>

<success_criteria>
- FlowService injectable with 4 signals: nodes, edges, activeNodeId, selectedNodeId
- FlowService.loadDefaultFlow() fetches /api/flow and populates signals
- GET /api/flow returns 8 nodes (all 6 types) and 8 edges
- @foblex/flow installed and FFlowModule importable
- Tailwind CSS v4 utility classes work
- SSR disabled, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-flow-editor/02-01-SUMMARY.md`
</output>
